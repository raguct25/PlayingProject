import AppNavigator from "./AppNavigator";
import React from "react";
import { Root } from "native-base";

const Boot = () => {
  return (
    <Root>
      <AppNavigator />
    </Root>
  );
};
export default Boot;
